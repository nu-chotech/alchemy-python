# 言葉錬成ラボ DESIGN

## 目的と操作例

この作品は、Word2Vec などの意味ベクトルを操作して、言葉から言葉への寄り道と目標への接近を楽しむゲームです。生成AIに結果語を作文させず、既存語彙の近傍語から結果を選びます。

1ターンの流れは、目標語を固定して開始し、素材Aと素材B、Aの割合 `alpha` を指定し、候補を最大3語見る、1語を確定する、確定語を次の素材に再利用する、という形です。候補を見ただけでは手数、履歴、コンボは変わりません。

## 既存機能と今回の実装

既存機能:

- `game.py`: 自作CLI基盤。`model/entity_vector.kv` を読み込んで、現在語と入力語から1語へ進めます。
- `semantic_alchemy.py` / `/api/games`: 既存の実験版。`mix` / `slerp` / `subtract` / `repel` / `purify` の演算、スコア、ターン制限があります。

今回の実装:

- `candidate_alchemy.py` / `/api/craft/...`: 素材A・B・割合から候補セットを作り、選択確定で状態を進める追加モードです。
- `static/index.html`: ローカル確認用のHTML UIです。候補カード、素材欄への再利用、簡易3Dマップを含みます。

今後の候補:

- 割合解析、意味軸による方向探針、慣性型コンボ、英語・多言語・文章Embeddingへの差し替え。
- 正規化しない連続ベクトルのユークリッド距離なら、割合の最適化を二次関数として扱えます。今回の実装は正規化ベクトルのコサイン類似度と近傍語選択なので、その最適化とは別です。

## 候補検索

追加モードの処理順:

1. 素材A `a`、素材B `b` のベクトルをそれぞれ正規化します。
2. `q = normalize(alpha * a + (1 - alpha) * b)` を作ります。
3. `q` に近い上位 `K=24` 語を候補プールにします。
4. プール内を次のスコアで並べ替え、最大3語を返します。

```text
score(w) = (1 - beta) * cos(v_w, q) + beta * cos(v_w, g)
```

`g` は開始時に固定した目標語です。目標への近さだけで全語彙を検索せず、まず合成位置 `q` の近傍語に絞ります。

仮パラメータ:

- `POOL_SIZE = 24`
- `CANDIDATE_COUNT = 3`
- ふつう: `beta = 0.03`
- やさしい: `beta = 0.12`
- コンボ補助: `combo * 0.025` を加算
- 上限: `beta <= 0.22`
- 目標補正OFF: `beta = 0`

ふつうモードは合成位置への近さが主で、候補選択前の目標距離はレスポンスに含めません。やさしいモードは素材に近い候補群の中で目標に近い語を弱く優先し、候補選択前にも目標距離を返します。目標に近いことと人間の直感に合うことは別の指標です。

## 例外の扱い

- 未対応語: `422 unsupported_word`。理由と利用できる候補語を返します。
- 同一素材: 許可します。ただし候補から素材語そのものは除外します。
- 入力語の除外: 素材A・Bは候補から除外します。
- 候補不足: `422 candidate_shortage`。
- ゼロベクトル: `422 zero_vector`。
- 同点: `score`、`q` への近さ、語の文字列の順で決定的に並べます。
- 乱数: 候補選択では使いません。`seed` は将来の比較用に開始APIで保持します。

## コンボ

最初の確定は比較の基準作りです。2回目以降、確定結果が前回の確定語より目標に近づいたらコンボを増やし、同距離または悪化でリセットします。コンボは次回候補の `beta` を少し上げるだけで、必ず近づく保証ではありません。開始時の `combo_enabled=false` で独立してOFFにできます。

## 高次元判定と3D表示

勝敗、目標距離、コンボ判定は元の高次元ベクトルのコサイン類似度で行います。3Dマップの座標は表示用です。

3D変換はゲーム開始時に、開始語、目標語、語彙の一部を使った固定PCAで作ります。新しい候補語や確定語にも同じ変換を適用し、候補のたびに地図全体が動かないようにしています。3D表示や意味軸は、人間の意味理解そのものを正確に表すものではありません。

## 状態遷移

```text
開始
  -> game_id / target / difficulty / current / nodes を返す
候補表示
  -> candidate_set_id / candidates を返す
  -> turn, combo, history は変えない
確定
  -> active candidate_set だけ受け付ける
  -> current, turn, history, combo を更新
  -> 同じ候補セットの二重確定と古い候補セットの確定を拒否
次の手
  -> 確定語を素材A/Bへ戻して再利用できる
```

## API

開始:

```http
POST /api/craft/games
Content-Type: application/json

{
  "target": "100億",
  "difficulty": "easy",
  "use_mock": true,
  "combo_enabled": true,
  "goal_bias_enabled": true,
  "seed": 7
}
```

候補作成:

```http
POST /api/craft/games/{game_id}/candidates

{
  "material_a": "りんご",
  "material_b": "金",
  "alpha": 0.5
}
```

確定:

```http
POST /api/craft/games/{game_id}/confirm

{
  "candidate_set_id": "...",
  "candidate_id": "..."
}
```

主要エラー:

- `400`: 目標語が語彙にない、難易度が不正。
- `404`: game_id や candidate_id が不明。
- `409`: 二重確定、古い候補セットの確定。
- `422`: 未対応語、ゼロベクトル、候補不足。
- `503`: 実モデルや `gensim` が利用できない。

## 役割分担

バックエンド:

- 目標、難易度、コンボ、候補セットの正当性を管理します。
- 難易度で隠すヒントはレスポンスにも含めません。
- 確定時だけゲーム状態を更新します。

フロント:

- 素材A/B、割合、難易度、設定を入力します。
- 候補カードの選択、確定、素材欄への再利用を提供します。
- 3Dマップの回転・ズーム・強調表示を担当します。カメラ操作ではゲーム状態を変更しません。

## ローカル起動

```bash
source .venv/bin/activate
uvicorn app:app --reload
```

URL:

- Web UI: http://127.0.0.1:8000/
- Swagger UI: http://127.0.0.1:8000/docs

停止は起動したターミナルで `Ctrl-C` です。

実モデルは `model/entity_vector.kv` を使います。txtモデルから作る場合:

```bash
source .venv/bin/activate
python convert.py
```

モデルがない環境では、開始フォームの `Mock` をONにします。実モデルの読み込み失敗を黙ってモックに切り替えることは、追加モードでは行いません。

## 確認済み事項と制約

確認済み:

- 既存 `/api/games` のテストと追加 `/api/craft` のテストは `unittest` で通過。
- 候補表示だけでは状態が進まない。
- 確定時だけ履歴、手数、コンボが更新される。
- ふつうモードでは候補の目標距離を返さず、やさしいモードでは返す。
- `goal_bias_enabled=false` で `beta=0` になる。
- 二重確定、古い候補セット、未対応語を拒否する。

制約と未確定事項:

- 確認用HTMLは本格フロントの前段で、永続化や複数ユーザー管理はありません。
- 3Dマップは軽量なDOM表示です。大量ノードの常時表示は想定していません。
- 勝利条件は追加モードでは「目標語と一致でクリア、手数は記録のみ」です。
